# Performance Lab — Все тексты пользовательского интерфейса

## HERO СЕКЦИЯ

### Терминальный статус
```
> system.init() | rust v1.78.0 | 5 задач загружено | статус: работает
```

### Заголовок
```
Performance Lab
5 задач на высокопроизводительный код
```

### Подзаголовок (subtitle)
```
Каждый челлендж — реальная задача системного программирования. Подход Naive
vs Optimized на Rust с анализом Big O, бенчмарками и
объяснением каждой оптимизации.
```

### Клавиатурные подсказки (keyboard hints)
```
Ctrl+K — Палитра команд
? — Горячие клавиши
E — Развернуть все
1-5 — Перейти к задаче
```

### Статистика героя (stats row)
```
5 — Задач
ОБЩЕЕ УСКОРЕНИЕ × — (значение, напр. 102×)
ПАМЯТЬ УЛУЧШЕНА — (значение, напр. 3/5)
RUST — Язык
```

### Быстрая статистика (quick stats)
```
Быстрая статистика
мин {N}× — (мин. ускорение, напр. 2.3×)
макс {N}× — (макс. ускорение, напр. 58.8×)
ср {N}× — (среднее ускорение, напр. 20.5×)
просмотрено {N}/5 — (если есть прогресс)
```

### Прогресс (progress timeline)
```
Прогресс
Просмотрено / Не просмотрено · Сложность
```

### Кнопка сброса прогресса
```
Сбросить прогресс
```

---

## НАВИГАЦИЯ (HEADER)

### Навигационные элементы
```
Обзор
#1
#2
#3
#4
#5
Методология
Вайб-гайд
Тепловая карта
Дашборд
Результаты
Итоги
```

### Breadcrumb (хлебные крошки)
```
PERF LAB > СЕКЦИЯ > #N ЗАДАЧА...
```

### Статус просмотра (в хедере)
```
{reviewed}/5 — (напр. 3/5)
{achievements} — (коло достижений)
```

### Кнопки управления (header buttons)
```
Сравнить задачи — иконка GitCompareArrows
Экспорт в Markdown — иконка Download
Палитра команд (Ctrl+K) — иконка Command
Поделиться URL — иконка Share2/Link2
Развернуть все / Свернуть все — иконки ChevronUp/ChevronDown
```

---

## ПОИСК И ФИЛЬТРАЦИЯ

### Строка поиска
```
Поиск по техникам, названиям или описаниям...
```

### Счетчик результатов поиска
```
{N} совпадение / совпадения / совпадений
```

### Пустое состояние поиска
```
Нет совпадений
Попробуйте другой запрос
```

### Фильтры сложности
```
Все ({count}) — напр. Все (5)
Продвинутый ({count}) — напр. Продвинутый (2)
Экспертный ({count}) — напр. Экспертный (3)
```

### Фильтр избранного
```
Избранное ({count})
```

---

## КАРТОЧКА ЗАДАЧИ (TASK CARD)

### Заголовок карточки
```
#{id} — (номер задачи, напр. #1)
{difficulty} — (Продвинутый / Экспертный)
{category} — (Алгоритмы / I/O / Конкурентность / Вычисления / Параллелизм)
{speedup}× — (ускорение, напр. 2.3×)
мin чтения — (время чтения, если раскрыто)
```

### Постановка задачи (problem statement)
```
Постановка задачи
{problem text}
```

### Ограничения (constraints)
```
Ограничения
- {constraint1}
- {constraint2}
...
```

### Табы кода (code tabs)
```
Базовый — (таб "Наивный")
Оптимизированный — (таб)
```

### Заголовки блоков кода
```
Наивный — {title}
Оптимизированный — {title}
```

### Описание блоков кода
```
{N} строк — (кол-во строк)
{N} символов · ~{N} токенов — (тултип)
```

### Анализ Big O
```
Анализ Big O
Базовый / Оптимизированный
Время: {complexity} — (напр. O(n) avg, O(n log n))
Память: {complexity} — (напр. O(n), O(1) extra)
{explanation}
```

### Ключевые оптимизации
```
Ключевые оптимизации
{01} {technique_name}
{description}
{02} {technique_name}
...
```

### Бенчмарки (BenchChart)
```
Бенчмарк
Ускорение
{speedup}×
Память
-{N}% / +{N}%
базовый — {time} / {memory}
оптимизированный — {time} / {memory}
```

### Предупреждение о компромиссе (tradeoff)
```
Компромисс по памяти: +{N} МБ в обмен на {N}× ускорение
```

---

## СРАВНЕНИЕ КОДА

### Заголовки режимов
```
Сравнение кода
```

### Переключатель режимов
```
Переключить на вкладки — иконка Rows3
Переключить на сравнение — иконка Columns2
```

### Переключить diff
```
Переключить diff
```

### Заголовки в diff режиме
```
Различия — {title}
+N / -N — (добавлено/удалено строк)
```

---

## РАЗДЕЛЫ (SECTIONS)

### Методология
```
Методология
```

### 8 принципов методологии
```
1. Big O оптимизация — Всегда выбирай минимальную сложность. O(1) > O(log n) > O(n) > O(n²)
2. Кэш-локальность — Data-Oriented Design: compact arrays, sequential access, AoS vs SoA
3. Минимизация аллокаций — Object pooling, arena allocators, pre-allocation, buffer reuse
4. Эффективный I/O — Async non-blocking I/O, buffering, batching системных вызовов
5. Lock-free конкурентность — Atomic operations, ring buffers, CAS вместо mutex
6. Zero-cost абстракции — Inline, generics, monomorphization — абстракции без runtime overhead
7. SIMD векторизация — AVX2/AVX-512: 8-16 элементов параллельно в одной инструкции
8. Профилирование — Измеряй, а не гадай. criterion, perf, flamegraph
```

### Теги техник
```
Теги техник
{technique_name}{count}
...
```

---

### Вайб-гайд
```
Вайб-гайд разработчика
Как перевести 5 задач с языка «ментального расстройства Rust-разработчика» на язык «комфортного веб-разработчика»
```

#### Предупреждение (warning banner)
```
Осторожно: мины замедленного действия
Задача 2 (CSV): Box::leak(mmap...) — утечка памяти. 
Задач�� 5 (Lock-free): unsafe impl Send/Sync — если ошибётесь 
хотя бы в одном Ordering, получите «призрачные» баги раз в месяц.
```

#### Мантра вайб-кодера
```
"Все эти оптимизации уже написаны грустными людьми на Rust и C++. 
Моя задача — найти их npm-пакет и импортировать."
— Мантра вайб-кодера на 2026
```

#### Перевод задач (5 карточек)

**Задача 1:**
```
Вайб кодер думает: {vibe_code}
Документ говорит: {doc}
Выжимка → {tip}
```

{translations for all 5 tasks}

#### Таблица соответствия (quick reference)
```
Быстрый справочник: Rust → Web
Концепция Rust | Эквивалент в Web | npm-пакет
```

#### Финальное правило
```
Единственное правило: Если твой Array.prototype.map().filter().reduce() 
на 50K элементов фризит фронтенд на секунду — вспомни Задачу 1 и 4. 
Не делай эту работу в JS. Отдай на бэк или в Web Worker.
```

---

### Тепловая карта
```
Тепловая карта влияния оптимизаций
Многомерный анализ задач — интенсивность показывает относительный эффект оптимизации (0–100)
Низкий — Высокий
```

#### Метрики тепловой карты
```
Скорость
Память
Кэш-локальность
Параллелизм
Сложность кода
```

---

### Дашборд
```
Дашборд
```

#### Статистика дашборда
```
Строк оптимизировано: {N} строк
Техник использовано: {N} уник.
Среднее ускорение: {N}×
Лучший результат: #{id} {N}×
Общее время сэкономлено: {N}s / {N}ms
Сложность снижена: {N}/{N} задач
```

#### Распределение ускорения
```
Распределение ускорения
#{id}: {speedup}×
```

---

### Результаты (Results Table)
```
Итоги
```

#### Заголовки таблицы
```
# | Задача | Базовый | Оптим. | Ускорение | Память
```

#### Значения таблицы
```
#{id} | {title} | {baseline_time} | {optimized_time} | {speedup}× | -{N}% / +{N}%
```

#### Легенда графика
```
Ускорение (×)
Экономия памяти (%)
```

### Многомерное сравнение (Radar Chart)
```
Многомерное сравнение
```

---

### Итоги (Summary)
```
Итоги
```

#### Карточки ускорений
```
#{id}
{speedup}×
```

#### Итоговое описание
```
8 принципов высокопроизводительного кода: Big O оптимизация → 
Кэш-локальность (Data-Oriented Design) → Минимизация аллокаций → 
Async I/O → Lock-free конкурентность → Zero-cost абстракции → 
SIMD векторизация → Профилирование («Измеряй, а не гадай»).
```

#### Итоговые метрики
```
Общее ускорение: {N}×
Память улучшена: {N}/5
```

---

## ПАНЕЛИ И МОДАЛЫ

### Системный монитор (System Monitor)
```
Системный монитор
Общее время сэкономлено: {N}s / {N}ms
Общая дельта памяти: -{N} MB / +{N} MB
Распределение ускорения
#{id}: {speedup}×
```

### Панель сравнения задач (Task Comparison Panel)
```
Сравнение задач
#{id}: {title}
#{id}: {title}
Сравнение ускорения
#{id}: {speedup}×
#{id}: {speedup}×
Дельта памяти
#{id}: -{N}% / +{N}%
#{id}: -{N}% / +{N}%
Сложность
#{id}: Время: {complexity} | Память: {complexity}
#{id}: Время: {complexity} | Память: {complexity}
Техники и категория
#{id}: {N} техник | {category} · {difficulty}
#{id}: {N} техник | {category} · {difficulty}
Относительная производительность
Ускорение / Баз. время (с)
```

### Палитра команд (Command Palette)
```
Введите команду или поисковый запрос...
Нет результатов
↑↓ — Навигация
↵ — Выбрать
Esc — Закрыть
```

#### Пункты палитры
**Навигация:**
```
Обзор — Перейти
#{id} {title} — Перейти
Методология — Перейти
Результаты — Перейти
Итоги — Перейти
```

**Действия:**
```
Развернуть все задачи
Свернуть все задачи
Экспорт в Markdown
Переключить сравнение
Фильтр избранных
```

### Модаль помощи (Help Modal)
```
Горячие клавиши
Ctrl+K — Открыть палитру команд
? — Показать горячие клавиши
E — Развернуть / свернуть все задачи
1-5 — Перейти к задаче 1-5
Esc — Закрыть диалоги

Совет: нажмите Ctrl+K в любой момент для быстрого доступа
```

### Уведомление о достижении (Achievement Toast)
```
Достижение разблокировано
{name}
{description}
```

---

## ЭЛЕМЕНТЫ УПРАВЛЕНИЯ

### Кнопка Back to Top
```
Наверх — (aria-label)
```

### Guided Tour кнопка (в футере)
```
Обзорная экскурсия — (title)
Запустить экскурсию — (aria-label)
```

### System Monitor переключатель (в футере)
```
Системный монитор — (title)
Переключить монитор — (aria-label)
```

### Футер
```
Performance Lab
Rust · SIMD · Lock-free · Zero-copy
```

---

## ЗАПРОС НА РАЗРЕШЕНИЕ

### Проверка: нужно ли мне извлечь также?
- Все тексты из всех компонентов?
- Тексты error states?
- Тексты loading states?
- Что-то конкретное?

Пожалуйста, уточните, и я извлеку полностью!